language: python
matrix:
  include:
    - python: 3.5
    - python: 3.6
    - python: 3.7
      dist: xenial
      sudo: true
  
install:
  - python --version
  - python setup.py install
  
script: 
  - python setup.py test
  
cache:
  - pip
  - directories:
    - "${PYENV_CACHE_PATH:-$HOME/.pyenv_cache}"
